apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Environment-agnostic base. Overlays patch this per environment.
resources:
  - namespace.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: ai-native-idp
    includeSelectors: false
