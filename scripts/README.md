# Scripts

Helper automation. Cross-platform where possible.

| Script | Purpose |
| --- | --- |
| `validate_structure.py` | Assert the expected repository structure exists (used by CI) |

```bash
python scripts/validate_structure.py
```
