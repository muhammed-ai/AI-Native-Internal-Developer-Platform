# Example module (PLANNED, Phase 2/3). This is a contract placeholder that shows
# the intended shape: a cloud-specific implementation (AKS/EKS/GKE) is selected by
# the calling stack, which passes its own provider. No resources are declared yet.

# Implementation intentionally omitted at the foundation stage.
# See terraform/modules/README.md for the module catalogue and rules.
