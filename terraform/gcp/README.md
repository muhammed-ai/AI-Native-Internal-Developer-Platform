# GCP stack (foundational — multi-cloud reach)

> Status: **PLANNED**, intentionally lightweight. Only `versions.tf` exists.

Demonstrates foundational multi-cloud reach. Intended minimal scope: GKE,
Artifact Registry, IAM, VPC, Secret Manager. Auth via GitHub OIDC → Workload
Identity Federation. Not built out beyond what is demonstrated.
